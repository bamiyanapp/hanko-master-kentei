{
  "name": "hanko-master-kentei",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lint": "commitlint --last --verbose",
    "test": "npm run test -w frontend && npm run test -w backend"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2"
  }
}
